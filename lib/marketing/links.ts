// Shared external links used by more than one marketing surface. Kept in one
// place so a repo move never leaves a stale URL in a component nobody thought
// to grep.

/** The public repo holding the Claude Code skills M.D.N Tech authors. */
export const TOOLKIT_REPO = "https://github.com/bugiiiii11/handoff";

/**
 * The registered entity behind every M.D.N Tech surface — the one legal
 * footprint the site claims. Both footers (EN and SK) render this exact
 * string; there is no second trading address.
 */
export const COMPANY_LEGAL_LINE =
  "M.D.N Tech FZE · Al Shmookh Business Center, One UAQ, UAQ Free Trade Zone, Umm Al Quwain, U.A.E. · License 7813";
