import { redirect } from 'next/navigation'

// Infrastructure evolved into TechKit (Session 33) — keep old bookmarks working.
export default function InfrastructureRedirect() {
  redirect('/command-center/techkit')
}
