// Promoted to components/product-pages/code-block.tsx (both page trees need
// it); this re-export keeps the existing /toolkit imports working.
export { CodeBlock } from "@/components/product-pages/code-block";
